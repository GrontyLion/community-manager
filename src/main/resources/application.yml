
mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: flag # 全局逻辑删除的实体字段名(since 3.3.0,配置后可以忽略不配置步骤2)
      logic-delete-value: 1 # 逻辑已删除值(默认为 1)
      logic-not-delete-value: 0 # 逻辑未删除值(默认为 0)
#  configuration:
#    map-underscore-to-camel-case: true # 开启驼峰命名规则(默认为 false)
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/community_manager?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: 123456
  mail:
    from: peaceforest2022@outlook.com


swing:
  default:
    theme:
      name: default
  themes:
    default: com.formdev.flatlaf.intellijthemes.FlatSolarizedLightIJTheme
    Arc - Orange: com.formdev.flatlaf.intellijthemes.FlatArcOrangeIJTheme
